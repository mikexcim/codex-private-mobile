# 安全说明

## 信任边界

电脑端服务只应绑定到本机的 Tailscale IPv4 地址，并只允许 Tailscale 地址段访问。每名使用者必须使用自己的 Tailscale 账号和独立 Tailnet，不能加入发布者或其他员工的网络。

成功连接手机端后，使用者可以查看任务、发送指令，并对命令执行和文件修改请求作出批准。这个权限等同于远程操作本机 Codex，请将手机、电脑、Tailscale 账号和连接地址都视为敏感资产。

## 使用要求

- 不要将服务端口映射到路由器或公网。
- 不要关闭 Windows 防火墙规则中的 Tailscale 地址限制。
- 不要在安装包、脚本或 GitHub Issue 中提交 API Key、访问令牌、连接地址或账号信息。
- 仅从本仓库 Release 获取 Codex 私人端，并使用 `SHA256.txt` 校验。
- Tailscale 客户端只从官方渠道获取。

## 报告问题

安全问题可能包含敏感信息时，请勿创建公开 Issue。请通过仓库所有者提供的私密联系方式报告。

